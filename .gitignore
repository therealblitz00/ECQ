data/*.csv
!data/.gitkeep
__pycache__/
*.pyc

# Local array cache and experiment outputs built from the (gitignored) raw data
data/cache/
data/derived/submission_*.csv

# Rendered notebook exports (regenerable via Jupyter's "Export as HTML")
notebooks/exports/

# Local audit & notes
agent_audit_notebook.yaml
resumo_crm_faturacao.md
